<h1>Stocks</h1>

<ul>
    <% @stocks.each do |stock| %>
        <li>
            <%= link_to stock.name, stock_path(stock) %><br>
            <%= stock.ticker %><br>
            <%= number_to_currency(stock.price) %><br>
        </li><br>
    <% end %>
</ul>

